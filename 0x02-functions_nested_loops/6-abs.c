#include "main.h"
/**
 * int _abs -  a function computes the absolute value of an integer.
 * @n: rntry num
 * Return: 0
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (n);
	}
}
