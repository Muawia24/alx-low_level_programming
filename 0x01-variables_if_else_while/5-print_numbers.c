#include <stdio.h>
/**
 * main - prints numbers 0 to 10
 * Return: 0 if nothing went wrong
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d", num);
	printf("\n");
	return (0);
}
